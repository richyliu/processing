/*
 * Key:
 * . spike
 * = block
 * - half block
 * Number after key indicates # of grids tall for blocks, or how far up the
 * block is for half blocks
 * Number preceeding key indicates number of spaces before
 * Spaces are used to seperate columns
 */
window.geodash_data = {
    // in units called grids
    level1: {
        spikes: '2. 3. 0. 4= 0. 0. 0. 0=2 0. 0. 0. 0=3',
        
    }
};