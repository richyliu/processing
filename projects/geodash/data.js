/*
 * Key:
 * . spike
 * = block
 * ^ pit spike
 * - half block
 * Number after key indicates # of grids tall
 * Number preceeding key indicates number of spaces before
 * Parentheses used to group vertical sections
 * Spaces are ignored by the parser
 */
window.geodash_data = {
    // in units called grids
    level1: [
        '5. 5. '
    ]
};